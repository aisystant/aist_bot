"""
Онбордер (DP.ROLE.067) — каркас модуля входа (WP-406 Ф5, «Фундамент»).

# see DP.SC.170, DP.ROLE.067

Онбордер доводит нового человека до состояния «Первокурсник» — закрывает две
характеристики готовности:
  Х2 — понимание сообщества (что за сообщество, роли, нормы, куда обращаться).
  Х3 — выбор траектории (ступень 1-5 + узкое место + первый курс).

Архитектура (консенсус peer-сессии 2026-06-11-07-wp406-onboarder-f5-impl):
  - Онбордер = тонкий ОРКЕСТРАТОР поверх существующих машин, не третья FSM.
  - Х3 делегируется готовому Диагносту R28 (handlers/diagnose.py): ступень +
    узкое место уже считаются, `recommended_stream` уже в проде
    (db/queries/cp_assessment.py). Онбордер транслирует поток в человеко-
    читаемый оффер курса (см. core/onboarder/x3.py).
  - Х2 — структурированный мини-тест из 4 вопросов в режиме teach-confirm
    (дай ориентацию → подтверди), БЕЗ fail-state: инвариант обещания
    «Онбордер не бросает» (DP.SC.170). См. core/onboarder/x2.py.
  - Хранилище: отметки завершения `x2_completed_at` / `x3_completed_at` в
    development.user_state (миграция 030, queryable для проактивных напоминаний
    и Ф6); черновики/last_gap — в current_context['onboarding']. См. storage.py.
  - Защита Dispatcher (3 уровня): (1) доступ/тир, (2) НЕ в активном SM-состоянии,
    (3) открыт разрыв Х2/Х3. Все три — иначе Онбордер не перехватывает.

Статус «Фундамент»: реальны слой хранилища (storage.py) и чтение статуса/разрыва
здесь. Боевая логика flow Х2/Х3 и подключение к Dispatcher — отдельными заходами
(complete vertical slice с критериями приёмки), поэтому should_handle/handle —
интерфейсные заглушки. Модуль НЕ подключён к живому роутингу: поведение бота не
меняется до реализации срезов.
"""

from core.onboarder import storage, x2, x3

__all__ = [
    "storage",
    "x2",
    "x3",
    "get_status",
    "has_open_gap",
    "should_handle",
    "handle",
]


async def get_status(chat_id: int) -> dict:
    """Готовность Первокурсника по двум характеристикам Онбордера.

    Returns:
        {"x2_done": bool, "x3_done": bool,
         "x2_completed_at": datetime | None, "x3_completed_at": datetime | None}
    """
    return await storage.get_status(chat_id)


async def has_open_gap(chat_id: int) -> bool:
    """Есть ли незакрытая характеристика Онбордера (Х2 или Х3).

    Только для пользователей, прошедших базовую регистрацию
    (onboarding_completed). Это третий уровень защиты Dispatcher —
    «зачем звать Онбордера». Без открытого разрыва Онбордер молчит.
    """
    status = await storage.get_status(chat_id)
    return not (status["x2_done"] and status["x3_done"])


async def should_handle(intern: dict, message) -> bool:
    """Три условия перехвата Онбордером (заглушка интерфейса — Ф5 срез).

    Контракт (консенсус 2026-06-11-07): перехватывать сообщение ТОЛЬКО когда
    одновременно:
      1. доступ/тир разрешён (через существующий tier/ServiceRegistry-чек);
      2. пользователь НЕ в активном SM-состоянии (консультация, заметки и пр.
         тоже дают current_state=None — нужен явный признак простоя, метода
         is_active_for_user в dispatcher пока НЕТ, заводится в срезе);
      3. has_open_gap(chat_id) истинно.

    Не реализовано в «Фундаменте» намеренно: условие (2) требует нового
    признака простоя SM, которого ещё нет. Поднимать половинчатый перехват
    опасно (риск прервать консультацию). Реализуется вместе с условием (2).
    """
    raise NotImplementedError(
        "should_handle: реализуется в срезе Ф5 вместе с признаком простоя SM "
        "(условие 2). См. docstring и report сессии 2026-06-11-07."
    )


async def handle(intern: dict, message) -> None:
    """Обработать ход Онбордера: довести открытый разрыв Х2/Х3 (заглушка).

    Диспетчерский хук: по открытому разрыву выбирает flow — x2.run_step (мини-
    тест 4 вопросов teach-confirm) либо x3.run (делегация Диагносту + оффер
    курса), фиксирует прогресс через storage. Реализуется срезами Х2 и Х3.
    """
    raise NotImplementedError(
        "handle: реализуется срезами Х2 (core/onboarder/x2.py) и "
        "Х3 (core/onboarder/x3.py)."
    )
