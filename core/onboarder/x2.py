"""
Х2 — понимание сообщества: мини-тест teach-confirm (WP-406 Ф5, «Фундамент»).

# see DP.SC.170, DP.ROLE.067

Х2 закрывается структурированным мини-тестом из 4 фиксированных вопросов
(карточка WP-406, критерий строки 122). Режим — teach-confirm («дай ориентацию →
подтверди»), БЕЗ fail-state: по инварианту обещания «Онбордер не бросает»
(DP.SC.170). Низкий ответ не «проваливает» человека — доставляет недостающий
кусок и идёт дальше. Отметка x2_completed_at ставится по факту прохождения всех
четырёх пунктов (storage.mark_x2_done), а не по порогу баллов.

«Фундамент»: X2_TOPICS — зафиксированный контракт 4 пунктов. run_step — заглушка
интерфейса flow (реализуется отдельным заходом с критериями приёмки).
"""

# Четыре пункта понимания сообщества (карточка WP-406, строка 122).
# Ключи — стабильные идентификаторы шага; человекочитаемые тексты ориентации
# и подтверждения наполняет срез Х2 (через i18n), не «Фундамент».
X2_TOPICS = (
    "community",      # (а) что такое IWE-сообщество
    "roles",          # (б) какие роли есть
    "norms",          # (в) нормы коммуникации
    "where_to_ask",   # (г) куда обращаться за помощью
)


async def run_step(intern: dict, message) -> None:
    """Один шаг мини-теста Х2 в режиме teach-confirm (заглушка интерфейса).

    Полный flow (отдельный заход):
      1. определить первый неподтверждённый пункт из X2_TOPICS
         (storage.get_onboarding_context → confirmed-набор);
      2. показать короткую ориентацию по пункту + чип «понятно» /
         «расскажи подробнее» (без fail-state);
      3. сохранить подтверждение в current_context['onboarding']
         (storage.save_onboarding_context);
      4. когда все 4 пункта подтверждены — storage.mark_x2_done(chat_id).
    """
    raise NotImplementedError(
        "run_step: flow мини-теста Х2 реализуется отдельным заходом "
        "(teach-confirm по X2_TOPICS, без fail-state)."
    )
